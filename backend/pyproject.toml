[project]
name = "url-shortener"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic>=2.0",
  "python-dotenv",
  "sqlalchemy>=2.0",
  "pydantic-settings>=2.14.2",
]

[tool.uv.scripts]
dev = "uv run python main.py"
